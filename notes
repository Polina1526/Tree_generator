Вид бинарника на выходе
    коалесценция (0):
        тип события(0) | в какой популяции | первый потомок | второй потомок
              0                  4                  53               70
    миграция (1):
        тип события(1) | из какой популяции | в какую популяцию | ID мигранта
              1                  4                   53               70

Запись данных в файл в виде 4-х чисел типа "unsigned int" (I) с доступным диапозоном от 0 до 65535,  ?????? Достаточно?
размер типа - стандартный(4 байта для I), порядок байт "little-endian" (<)
    import struct
    struct.pack("<5i", 0, pop_ID, first_offspring, second_offspring, time)   (ex. for coalescence)
    struct.pack("<5i", 1, source_pop_ID, dist_pop_ID, migrant_ID, time)      (ex. for migration)

?????? Имеет ли смысл сделть разный размер записсываемых байтов?
           например: "<c3I" (char, 3 * unsigned int)


!!!!!!!!!!!!!!!!! При коалесценции заменять потомков на их предка с номером большим, чем current_number_of_samples,
                      то есть фактически создавать нового (найденного) образца со своим новым, исключительным ID
                            current_max_sample_id